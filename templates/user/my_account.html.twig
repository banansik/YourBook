{% include 'default/_form.html.twig' with {
    page_title: 'user_edit',
    attr: {
        method: 'PUT',
        action: url('user_my_account', {'id': user.id}),
    },
    form: form,
    back_to_list_path: 'user_index',
} %}